// web-client — Web Client
// Scope: Browser SPA that renders an interactive Sudoku board, allows difficulty selection, fetches puzzles from Puzzle Service, submits completed boards for validation, and displays leaderboard from Scores Service. Handles network errors gracefully.
// Owns: packages/web
// This team builds its slice here each phase.
export {};
