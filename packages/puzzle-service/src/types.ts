// packages/puzzle-service/src/types.ts
// Re-export shared contract types for convenience within the puzzle-service package.

export type { Board, Difficulty, PuzzleRequest, PuzzleResponse, ValidateRequest, ValidateResponse } from '@init-sudoku-post7/contracts';
