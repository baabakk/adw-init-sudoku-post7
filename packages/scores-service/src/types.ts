import { Difficulty, ScoreRequest, ScoreResponse, LeaderboardResponse } from "../../contracts/src/index";

// Re-export the contract types for local use
export type { Difficulty, ScoreRequest, ScoreResponse, LeaderboardResponse };
