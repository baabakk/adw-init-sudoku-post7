// puzzle-service — Puzzle Service
// Scope: Generates valid, uniquely-solvable Sudoku puzzles per difficulty and validates submitted boards. Provides GET /puzzle and POST /validate endpoints. Stateless, no database.
// Owns: packages/puzzle-service
// This team builds its slice here each phase.
export {};
